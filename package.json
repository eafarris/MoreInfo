{
  "name": "more-info",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "css:watch": "tailwindcss -i src/input.css -o src/style.css --watch",
    "css:build": "tailwindcss -i src/input.css -o src/style.css --minify",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.2.1",
    "@tailwindcss/typography": "^0.5.19",
    "@tauri-apps/cli": "^2",
    "tailwindcss": "^4.2.1"
  }
}
